version: "3.7"

services:
  web:
    build: .
    environment:
      - PORT=3000
      - MONGODB_URI=mongodb://mongo:27017/nodegoat
    command: sh -c "sleep 5 && node server.js"
    ports:
      - "4000:3000"

  mongo:
    image: mongo:4.4
    user: mongodb
    expose:
      - 27017
